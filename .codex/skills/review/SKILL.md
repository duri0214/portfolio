---
name: review
description: このリポジトリでPRレビュー、レビューコメント確認、指摘対応、スラッシュコマンド風のレビュー作業を行うときに使う。レビュー結果を提示した後、未解決の実行可能な指摘を追加確認なしで修正し、確認、コミット、push、元コメントへの返信まで進める。repository/service/valueobject の棲み分けも確認する。
---

# Review

## 概要

このスキルは、このリポジトリでPRレビューやレビューコメント対応を行うための手順をまとめる。レビュー結果を先に提示するが、実行可能な指摘がある場合はそこで停止せず、修正、確認、コミット、push、元コメントへの返信まで続ける。

## 手順

1. レビュー対象のPRを特定する。
   - ユーザーがPR番号やURLを指定している場合はそれを使う。
   - 指定がない場合は、現在のブランチから `gh pr view --json number,url` で確認する。
   - `gh` の認証が無効な場合は、GitHubの読み書きを止めて `gh auth login -h github.com` が必要だと伝える。
2. レビューコメントをスレッド単位で確認する。
   - 未解決レビュー、インラインコメント、requested changes が関係する場合は `github:gh-address-comments` を使う。
   - `gh pr view --comments` だけで「コメントなし」と判断しない。この表示ではインラインレビューコメントを取得できない場合がある。
   - `github:gh-address-comments` が利用できない場合は、少なくとも以下を実行してレビュー本文とインラインコメントを別々に取得する。
     - `gh api repos/<owner>/<repo>/pulls/<PR番号>/reviews --paginate`
     - `gh api repos/<owner>/<repo>/pulls/<PR番号>/comments --paginate`
   - インラインコメントでは `id`、`in_reply_to_id`、`pull_request_review_id`、`body`、`path`、`line`、`html_url` を確認する。レビュー本文の `state` だけではインラインコメントの有無や対応状況を判定しない。
   - 解決済みかどうかが必要な場合は、`github:gh-address-comments` のスレッド取得手順、またはGitHub GraphQLの `reviewThreads` を使ってスレッド状態を確認する。
   - レビュー取得結果が空でも、レビュー対象PRのAPIレスポンスと取得コマンドを確認するまで「コメントなし」と結論づけない。
   - 対応が必要なコメントを、ファイル別または挙動別にまとめる。
   - 解決済み、重複、情報共有のみのコメントは、ユーザーが求めていない限り対応対象から外す。
   - 未解決で内容が明確な実行可能コメントは、ユーザーへの追加確認なしにすべて対応対象とする。
   - コメントの要求が曖昧、相互に矛盾、または破壊的な操作を伴う場合だけ、確認してから修正する。
   - 指摘を重大度順に整理したレビュー結果を commentary で先に提示する。実行可能な指摘が残っている場合も、提示後に手順を続行する。
3. レイヤーの棲み分けを確認する。
   - Repository: 永続化、クエリ、外部ストレージや外部APIの集合アクセスを担当する。
   - Service: 処理手順やドメイン操作を担当する。Repository境界がある場合は、Service内で場当たり的にDjangoモデルを直接クエリしない。
   - Value Object: dataclass や値の意味を持つ型を置く。新しいValue ObjectをServiceやRepositoryファイルへ混在させない。
4. ローカルで修正する。
   - 各修正がどのレビューコメントに対応するか追えるようにする。
   - 関係ないリファクタリングへ広げない。
   - 初期実装や試行錯誤で入れた残骸を残さず、最終的に必要な実装だけへ整理する。モデル修正を伴う仮実装の後始末には、migration の作成・削除・適用確認まで含める。
   - KISS と YAGNI を意識し、後方互換や抽象化を「今回PR内で消えた実装」のためだけに残さない。
   - 既に作業ツリーにあるユーザー変更は壊さない。
5. 確認する。
   - 変更したアプリや挙動に絞ってテストを実行する。
   - テストを実行できない場合は、理由を報告する。
   - ドキュメントのみの変更は `git diff --check` を最低限実行する。
6. 変更を反映する。
   - ユーザーが停止を指示していない限り、確認成功後にコミット、push、PR更新まで進める。
   - コミット前に作業ツリーと差分を確認し、今回のレビュー対応に関係する変更だけを含める。
7. レビューコメントの指摘を修正した場合は、対応済みコメントへ返信する。
   - 原則として、元のインラインレビューコメントに `gh` で返信する。
   - 可能なら `in_reply_to_id` が元コメントを指すスレッド返信にする。
   - どのコミットまたは変更で対応したかを簡潔に残す。
   - コメントが outdated などで直接返信できない場合だけ、元コメントURLと対応内容をPRコメントに書く。
   - REST APIで返信する場合は、`POST /repos/<owner>/<repo>/pulls/<PR番号>/comments` に `in_reply_to` を付ける。`/pulls/comments/<comment_id>/replies` が404でも、返信不能とは判断しない。
   - 返信後は `gh api repos/<owner>/<repo>/pulls/<PR番号>/comments --paginate` で `in_reply_to_id` と返信本文を確認する。

## レビュー結果の書き方

レビューとして回答するときは、重大度順に指摘を先に書き、ファイルと行番号を添える。問題が見つからない場合は明確にそう書き、残るテスト不足やリスクがあれば短く補足する。
